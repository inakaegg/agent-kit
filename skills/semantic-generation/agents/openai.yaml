interface:
  display_name: "Semantic Generation"
  short_description: "Fix the referents before the words, so design documents and naming stay consistent"
  default_prompt: "Use $semantic-generation to build the correspondence table first, then write this design document."
