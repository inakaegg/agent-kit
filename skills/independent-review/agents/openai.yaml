interface:
  display_name: "Independent Review"
  short_description: "Review non-trivial changes rigorously from a context independent of the implementation"
  default_prompt: "Use $independent-review to review this change from a fresh context."
