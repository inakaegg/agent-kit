interface:
  display_name: "PR Review Loop"
  short_description: "Converge review findings and CI on a PR, one latest head at a time"
  default_prompt: "Use $pr-review-loop to triage and converge this authorized PR."
