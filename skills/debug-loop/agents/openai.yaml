interface:
  display_name: "Debug Loop"
  short_description: "Isolate non-obvious defects and regressions down to root cause from repro, evidence, and history"
  default_prompt: "Use $debug-loop to investigate this failure from evidence."
